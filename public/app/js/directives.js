'use strict';

define(['angular'], function (angular) {
/*
    angular.module('beerApp.directives', ['beerApp.services'])
        .directive('appVersion', ['version', function (version) {
            return function (scope, elm, attrs) {
                elm.text(version);
            };
        }]);
*/
});
